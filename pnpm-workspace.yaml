# Puppeteer's postinstall downloads the pinned Chrome build that `pnpm perf`
# drives. It is the one dependency here allowed to run install scripts.
allowBuilds:
  esbuild: false
  puppeteer: true
  sharp: false
  unrs-resolver: false
